apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: mongo-user
  namespace: mern-db
spec:
  serviceName: "mongo-user"
  replicas: 1
  selector:
    matchLabels:
      app: mongo-user
  template:
    metadata:
      labels:
        app: mongo-user
    spec:
      containers:
      - name: mongo
        image: mongo:6.0
        ports:
        - containerPort: 27017
        volumeMounts:
        - name: mongo-data
          mountPath: /data/db
  volumeClaimTemplates:
  - metadata:
      name: mongo-data
    spec:
      accessModes: [ "ReadWriteOnce" ]
      resources:
        requests:
          storage: 1Gi
---
apiVersion: v1
kind: Service
metadata:
  name: mongo-user
  namespace: mern-db
spec:
  ports:
  - port: 27017
    targetPort: 27017
  selector:
    app: mongo-user
  clusterIP: None
