apiVersion: apps/v1
kind: Deployment
metadata:
  name: mongodb-auth
  namespace: hipster
spec:
  selector:
    matchLabels:
      app: mongodb-auth
  template:
    metadata:
      labels:
        app: mongodb-auth
    spec:
      containers:
      - name: mongodb
        image: mongo:6.0
        ports:
        - containerPort: 27017
---
apiVersion: v1
kind: Service
metadata:
  name: mongodb-auth
  namespace: hipster
spec:
  selector:
    app: mongodb-auth
  ports:
  - port: 27017
    targetPort: 27017
