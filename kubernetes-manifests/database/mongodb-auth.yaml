apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: mongodb-auth
  namespace: hipster
spec:
  serviceName: mongodb-auth-headless
  replicas: 1
  selector:
    matchLabels:
      app: mongodb-auth
  template:
    metadata:
      labels:
        app: mongodb-auth
    spec:
      containers:
      - name: mongodb
        image: mongo:6.0
        ports:
        - containerPort: 27017
        volumeMounts:
        - name: data
          mountPath: /data/db
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 300m
            memory: 256Mi
  volumeClaimTemplates:
  - metadata:
      name: data
    spec:
      accessModes: ["ReadWriteOnce"]
      resources:
        requests:
          storage: 1Gi
---
# Headless service for stable DNS within StatefulSet
apiVersion: v1
kind: Service
metadata:
  name: mongodb-auth-headless
  namespace: hipster
spec:
  clusterIP: None
  selector:
    app: mongodb-auth
  ports:
  - port: 27017
    targetPort: 27017
---
# ClusterIP service so other services can reach it by name
apiVersion: v1
kind: Service
metadata:
  name: mongodb-auth
  namespace: hipster
spec:
  selector:
    app: mongodb-auth
  ports:
  - port: 27017
    targetPort: 27017
