apiVersion: apps/v1
kind: Deployment
metadata:
  name: mongodb-catalog
  namespace: hipster
spec:
  selector:
    matchLabels:
      app: mongodb-catalog
  template:
    metadata:
      labels:
        app: mongodb-catalog
    spec:
      containers:
      - name: mongodb
        image: mongo:6.0
        ports:
        - containerPort: 27017
---
apiVersion: v1
kind: Service
metadata:
  name: mongodb-catalog
  namespace: hipster
spec:
  selector:
    app: mongodb-catalog
  ports:
  - port: 27017
    targetPort: 27017
