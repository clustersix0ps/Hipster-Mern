apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: mongodb-catalog
  namespace: hipster
spec:
  serviceName: mongodb-catalog-headless
  replicas: 1
  selector:
    matchLabels:
      app: mongodb-catalog
  template:
    metadata:
      labels:
        app: mongodb-catalog
    spec:
      containers:
      - name: mongodb
        image: mongo:6.0
        ports:
        - containerPort: 27017
        volumeMounts:
        - name: data
          mountPath: /data/db
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 300m
            memory: 256Mi
  volumeClaimTemplates:
  - metadata:
      name: data
    spec:
      accessModes: ["ReadWriteOnce"]
      storageClassName: "nfs"
      resources:
        requests:
          storage: 1Gi
---
apiVersion: v1
kind: Service
metadata:
  name: mongodb-catalog-headless
  namespace: hipster
spec:
  clusterIP: None
  selector:
    app: mongodb-catalog
  ports:
  - port: 27017
    targetPort: 27017
---
apiVersion: v1
kind: Service
metadata:
  name: mongodb-catalog
  namespace: hipster
spec:
  selector:
    app: mongodb-catalog
  ports:
  - port: 27017
    targetPort: 27017
