{
  "name": "frontend",
  "version": "1.0.0",
  "scripts": {
    "start": "npm --prefix client run dev",
    "build:client": "npm --prefix client ci && npm --prefix client run build",
    "build:image": "npm run build:client",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {}
}
